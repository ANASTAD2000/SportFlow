create database SportFlow ;
use SportFlow ;

CREATE table members (
id int primary key,
nom varchar (200),
date_de_naissance date,
sport_partiqui varchar(2000)
);
create table Entraineur (
nom varchar(200), 
specialite varchar (200)
);
create table Seance (


);
 